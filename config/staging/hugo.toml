# ==============================================================================
# BORTO.LOG — hugo.toml
# ==============================================================================

baseURL = "https://staging.bortoloso.me/"

[params]
  # Ambiente — ativa OpenGraph, Twitter Cards, Schema.org e Google Analytics
  # OBRIGATÓRIO para meta tags de redes sociais funcionarem
  env = "staging"